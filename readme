A function that lets you draw svg paths onto a webgl canvas

usage
        path.init(document.querySelector('canvas').getContext('webgl'))
        .draw('m 0 0 l 0 10 10 10 30 30 z')

TODO:
close concaves
implement path attributes
antialiasing (fxaa, supersampling, mitre joins)
fill concaves
dont fill holes in concaves
  tessellate shape into triangles, create triangles to fill shape
  add ear clipping + constrained delaunay 
implement rest of path spec
allow custom shaders
2d fallback for phones

??
look at nile
